import 'package:flutter/material.dart';

class MainAppStyle {
  // font family
  ThemeData themeData = ThemeData(
    fontFamily: 'Montserrat',
    primarySwatch: Colors.blue,
  );

  // MAIN Color
  Color mainColor = Color.fromRGBO(18, 119, 192, 1);
  Color secondColor = Color.fromRGBO(17, 40, 74, 1);
  Color colorText = Color.fromRGBO(43, 43, 43, 1);

  // bodyBG
  Color bodyBG = Color.fromRGBO(247, 249, 251, 1);

  // buttonBG
  Color buttonBG = Color.fromRGBO(77, 183, 74, 1);
  Color buttonTextColor = Colors.white;
}
